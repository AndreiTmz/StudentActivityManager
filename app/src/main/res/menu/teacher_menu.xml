<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/addStudent"
        android:title="@string/addUser"
        app:showAsAction="ifRoom"
        android:icon="@android:drawable/ic_input_add"/>

    <item
        android:id="@+id/viewStudents"
        android:title="@string/viewStudents"
        app:showAsAction="never" />

    <item
        android:id="@+id/saveStudents"
        android:title="@string/saveStudents"
        app:showAsAction="never"
        />
    <item
        android:id="@+id/viewGraphic"
        android:title="@string/viewGraphic"
        app:showAsAction="never"
        />



</menu>